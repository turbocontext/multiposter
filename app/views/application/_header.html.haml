.row.header
  .span15
    %a{href: root_path}
      %h2.fg-color-blue.logo
        Multiposter
        %i.icon-comments-4
  - if signed_in?
    .span12
      %a{href: "#"}
        %h3.to_right
          %i.icon-user
          = "#{current_user.email}"
    .span4
      %a{href: destroy_user_session_path, "data-method"=> "delete", rel: "nofollow"}
        %h3.to_right.fg-color-blue
          Sign out
          %i.icon-exit
  - else
    = link_to "Sign in", new_user_session_path
